; Production PHP settings for Railway deployment.
; Nginx client_max_body_size is 50M — keep upload_max_filesize aligned and post_max_size above that ceiling.
upload_max_filesize = 50M
post_max_size       = 60M
max_file_uploads    = 20
memory_limit        = 256M
max_execution_time  = 60
